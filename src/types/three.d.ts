// three.js ships its own TypeScript definitions.
// This file is intentionally kept for future local module augmentations if needed,
// but must NOT redeclare the whole module (it would override upstream types).
export {}
